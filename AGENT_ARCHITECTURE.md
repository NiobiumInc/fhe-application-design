# FHE Design Agent — Architecture Specification

**This document has moved to the [`fhe-design-agent`](../fhe-design-agent/AGENT_ARCHITECTURE.md) repository.**

The FHE Design Agent is a separate project that *consumes* the skill in this
repo (SKILL.md + references/) as read-only knowledge. See the new repo for
the full architecture spec, implementation, and application strategies.
